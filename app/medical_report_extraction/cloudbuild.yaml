steps:
  # build the container image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "europe-west1-docker.pkg.dev/$_PROJECT_ID/cloud-run-source-deploy/medical_report_extraction:$COMMIT_SHA",
        "./app/medical_report_extraction/",
      ]
    # push the container image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "push",
        "europe-west1-docker.pkg.dev/$_PROJECT_ID/cloud-run-source-deploy/medical_report_extraction:$COMMIT_SHA",
      ]
    # deploy to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "$_SERVICE_NAME",
        "--image",
        "europe-west1-docker.pkg.dev/$_PROJECT_ID/cloud-run-source-deploy/medical_report_extraction:$COMMIT_SHA",
        "--region",
        "europe-west1",
        "--platform",
        "managed",
        "--allow-unauthenticated",
         "--set-env-vars", 
        "CONFIG_FILE=$_CONFIG_FILE"
      ]
options:
  logging: CLOUD_LOGGING_ONLY
